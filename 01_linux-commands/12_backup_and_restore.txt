# Command: rsync
# default port: 873

sudo apt install rsync -y

# backup to remote server
rsync -av /path/to/mydirectory user@backup_server:/path/to/backup/directory

# option: list-only > only list shares and files
rsync --list-only <target_IP>::

rsync --list-only <target_IP>::<share>
rsync <target_IP>::<share><file> <file> # downloads file
